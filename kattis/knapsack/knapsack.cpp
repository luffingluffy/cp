#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
typedef pair<int, int> ii;
typedef vector<int> vi;
typedef vector<ii> vii;

int main() {
    int c, n;

    while (cin >> c && cin >> n) {
        vector<vi> object_list;
        for (int i = 0; i < n; i++) {
            vi object;
            int v, w;
            cin >> v >> w;
            object.push_back(v);
            object.push_back(w);
        }

    return 0;
    }
}